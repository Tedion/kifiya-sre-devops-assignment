---
# Application server configuration

- name: Install Python and pip
  package:
    name:
      - python3
      - python3-pip
    state: present

- name: Create application user
  user:
    name: "{{ app_user }}"
    group: "{{ app_group }}"
    create_home: yes
    state: present
